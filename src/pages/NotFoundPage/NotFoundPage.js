import {Alert} from 'react-bootstrap';

const NotFoundPage = () => {
    return (
        <div>
            <Alert variant={'info'} className={'text-center'}>Page was no found =(</Alert>
        </div>
    );
};

export {NotFoundPage};
